<h1>本の編集</h1>
<% if @book.errors.any? %>
 <%= @book.errors.count %>errors prohibited this obj from being saved:
 <% @book.errors.full_messages.each do |message| %>
  <ul>
  </ul>
  <%= message %>
 <% end %>
<% end %>

<%= form_with model:@book, local:true do |f| %>
 <label for="inputName">タイトル</label>
 <%= f.text_field :title,  autofocus: true, id:"inputName", placeholder:"名前"%>
 <label for="inputName">感想</label>
 <%= f.text_area :body,  autofocus: true, id:"inputName", placeholder:"感想"%>
 <%= f.submit '更新', class:"btn btn-success" %>
 <% if flash[:notice] %>
  <p id="notice">
   <%= flash[:notice] %>
  </p>
 <% end %>
 <%= link_to '詳細', book_path %>
 |
 <%= link_to '戻る', books_path %>
<% end %>